export const environment = {
  production: true,
  // Set this to your Render service URL to make Render the PRIMARY backend,
  // e.g. 'https://zech-news.onrender.com'. The app automatically falls back to
  // the Vercel "/api/*" functions if Render is cold/slow/down.
  // Leave '' to use Vercel only. (See DEPLOY.md.)
  apiBase: '',
};
